<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <artifactId>fenced-lock-lock-ownership-lost-exception</artifactId>
    <name>CP Subsystem - Reentrant Lock Acquire Failure on Session Close</name>

    <description>
        CP Subsystem - A lock holder fails on new lock() call if its CP session is closed
    </description>

    <parent>
        <groupId>com.hazelcast.samples.cp</groupId>
        <artifactId>cp-subsystem</artifactId>
        <version>0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <properties>
        <!-- needed for checkstyle/findbugs -->
        <main.basedir>${project.parent.parent.basedir}</main.basedir>
    </properties>
</project>
