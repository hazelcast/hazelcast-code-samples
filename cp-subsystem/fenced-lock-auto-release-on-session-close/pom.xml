<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <artifactId>fenced-lock-auto-release-on-session-close</artifactId>
    <name>CP Subsystem - Auto-release of FencedLock on Session Close</name>

    <description>
        CP Subsystem - FencedLock is auto-released when the lock holder's CP session is closed
    </description>

    <parent>
        <groupId>com.hazelcast.samples.cp</groupId>
        <artifactId>cp-subsystem</artifactId>
        <version>5.3.6-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <properties>
        <!-- needed for checkstyle/findbugs -->
        <main.basedir>${project.parent.parent.basedir}</main.basedir>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>
</project>
