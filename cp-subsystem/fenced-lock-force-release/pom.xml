<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <artifactId>fenced-lock-force-release</artifactId>
    <name>CP Subsystem - Force-release FencedLock</name>

    <description>
        CP Subsystem - FencedLock is force-released by the lock holder's session is closed via the API
    </description>

    <parent>
        <groupId>com.hazelcast.samples.cp</groupId>
        <artifactId>cp-subsystem</artifactId>
        <version>0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <properties>
        <!-- needed for checkstyle/findbugs -->
        <main.basedir>${project.parent.parent.basedir}</main.basedir>
    </properties>
</project>
